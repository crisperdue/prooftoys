// Declarations file to cut down on complaints from Typescript.

interface Window {
  _paq: Array;
  $: function;
  proofEditor: any;
  rCounter: number;
  dCounter: number;
}

interface Element {
  handleResize: function;
}
